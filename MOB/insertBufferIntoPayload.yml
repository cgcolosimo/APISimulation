schema: SimV1

services:
  - name: insertBufferIntoPayload 
    steps:
    - direction: Out
      name: Natürliche Personen übernehmen Request
      to: echo
      buffer:
        - name: Geburtsdatum
          value: '{DATE[][-32y][yyyy-MM-dd]}'

      message:
        method: POST
        payloadFile: mobpayload.json
